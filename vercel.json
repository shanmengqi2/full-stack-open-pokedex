{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist"
      }
    }
  ],
  "routes": [
    {
      "src": "/version",
      "dest": "/api/version.js"
    },
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}